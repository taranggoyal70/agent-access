import Link from "next/link";

export default function DocsPage() {
  return <main className="docs-page"><aside><Link href="/" className="wordmark">Agent Access</Link><nav><a href="#discover">Discover</a><a href="#register">Register</a><a href="#invoke">Invoke</a><a href="#mcp">MCP</a><a href="#receipts">Receipts</a></nav></aside><article><p className="kicker">Machine documentation</p><h1>Build for agent customers</h1><p>Agent Access gives every published sandbox a discoverable contract, first-class agent accounts, delegated credentials, and verifiable execution receipts.</p><h2 id="discover">1. Discover</h2><pre>GET /.well-known/agent-access/&#123;sandbox-slug&#125;</pre><h2 id="register">2. Register a sandbox agent</h2><pre>{`POST /api/agent/v1/{sandbox-slug}/accounts\nContent-Type: application/json\n\n{"name":"My Agent","requested_capabilities":["create_project"]}`}</pre><h2 id="invoke">3. Invoke with idempotency</h2><pre>{`POST /api/agent/v1/{sandbox-slug}/invoke/create_project\nAuthorization: Bearer aa_sbx_...\nIdempotency-Key: unique-run-id\n\n{"name":"Launch","key":"launch"}`}</pre><h2 id="mcp">4. MCP</h2><p>Use the published <code>/mcp/&#123;sandbox-slug&#125;</code> endpoint. Tool calls require the same bearer credential.</p><h2 id="receipts">5. Verify receipts</h2><p>Every successful invocation returns a signed receipt. Retrieve its public verification record from <code>/api/receipts/&#123;receipt-id&#125;</code>.</p></article></main>;
}
